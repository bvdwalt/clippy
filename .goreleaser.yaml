version: 2

project_name: clippy

before:
  hooks:
    - go mod download

builds:
  - main: ./cmd/clippy
    binary: clippy
    env:
      - CGO_ENABLED=1
      - >-
        {{- if eq .Os "darwin" }}
          {{- if eq .Arch "amd64" }}CC=zig cc -target x86_64-macos
          {{- else }}CC=zig cc -target aarch64-macos
          {{- end }}
        {{- else if eq .Os "linux" }}
          {{- if eq .Arch "amd64" }}CC=zig cc -target x86_64-linux-musl
          {{- else }}CC=zig cc -target aarch64-linux-musl
          {{- end }}
        {{- end }}
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    files:
      - LICENSE

homebrew_casks:
  - repository:
      owner: bvdwalt
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/bvdwalt/clippy"
    description: "Terminal clipboard history manager"
    license: "MIT"
    hooks:
      post:
        install: |
          system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/clippy"]

checksum:
  name_template: checksums.txt

release:
  github:
    owner: bvdwalt
    name: clippy

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
